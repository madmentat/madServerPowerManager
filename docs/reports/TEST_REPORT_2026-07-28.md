# Отчёт о тестах

## Сборка

- Ubuntu 25.04, GCC 14.2.0, CMake 3.31.6.
- Release build завершён.
- Криптографические тесты SHA-256, HMAC-SHA256, AES-128, AES-GCM пройдены.

## FSM

CTest: 1/1 passed. Внутри теста проверено 27 инвариантов:

- 25 обязательных сценариев ТЗ;
- дополнительный инвариант: `armed=false` не выдаёт управляющее действие даже
  из сохранённого `SHUTDOWN_REQUESTED`.
- после краткого отключения без снятия питания FSM не отправляет лишний `ON`.

## Интеграция, read-only

- `--validate-config`: успешно, `armed=false`.
- `--test-nut`: `OL`, заряд 100%, 54.0 В, вход/выход 230 В, нагрузка 4%.
- `--test-plug`: розетка включена; команды питания не отправлялись.
- `--dry-run`: переход `STARTING -> MONITOR_ONLY`, действий нет.
- `--status`: NUT, Proxmox и розетка доступны.
- симуляции OB/OL выдали ожидаемые переходы.
- после restart NUT и restart сервиса восстановился `MONITOR_ONLY`, все три
  systemd-сервиса активны, API healthy.

Реальный разряд, shutdown Proxmox и отключение розетки в рамках этого этапа не
проводились.
