# Отчёт обследования, 2026-07-28

Обследование выполнено до управляющих изменений. Proxmox не выключался,
розетка не переключалась, BIOS и конфигурация Proxmox не изменялись.

## Intel NUC

- Узел: `Mad-nPC1`, `192.168.88.148`.
- Модель: Intel D54250WYK, x86-64.
- ОС: Ubuntu 25.04, ядро `6.14.0-37-generic`.
- systemd: 257.4.
- Время: Europe/Moscow; NTP active; clock synchronized.
- Сеть: `eno1=192.168.88.148/24`, gateway `192.168.88.1`;
  DNS `192.168.88.244`, `192.168.88.1`.
- Proxmox `192.168.88.45`: ICMP и TCP/22 доступны.
- Tuya `192.168.88.73`: ICMP и TCP/6668 доступны.
- До миграции пакеты NUT отсутствовали.
- ИБП: USB bus 001, port 2, VID:PID `0001:0000`, USB 2.00,
  full-speed 12 Мбит/с, HID interface, два interrupt endpoints 0x02/0x82,
  пакет 8 байт, интервал 10 мс.
- Kernel path: `/sys/bus/usb/devices/1-2`; `hidraw0`, `hiddev0`.
- До установки NUT node `/dev/bus/usb/001/002` имел `0664 root:root`.
- Штатное правило NUT содержит `0001:0000`, `MODE=664`, `GROUP=nut`.
- После reload/trigger: `0664 root:nut`.
- Host key NUC: ED25519
  `SHA256:QvOB5EuqD+Iv5j9CWz8HBfg4u06jutmAFPwuLMBB1rg`.

## Proxmox

- Узел: `ard-s`, `192.168.88.45`.
- ОС: Debian 13, Proxmox VE 9.0.11, kernel `6.14.11-4-pve`.
- NUT: 2.8.1-5 (`nut`, `nut-client`, `nut-server`).
- Режим: `standalone`.
- Актуальный драйвер FinePower:
  `nutdrv_qx`, `vendorid=0001`, `productid=0000`,
  `protocol=hunnox`, `langid_fix=0x0409`, `novendor`,
  `noscanlangid`, `bus=001`.
- Исторический успешный запуск 2026-07-23:
  `Using protocol: Hunnox 0.02`, `Startup successful`,
  батарейный пакет 48 В, измерение 54.1–54.2 В.
- `battery.runtime` не вычислялся: `runtimecal not set`.
- После физического переноса USB на NUC драйвер Proxmox закономерно находится
  в auto-restart и сообщает `No supported devices found`.
- `upsd` и `upsmon` на Proxmox продолжают работать, но UPS stale/disconnected.
- Пользователь upsmon и пароль существовали; пароль в отчёт не копировался.
- `POWEROFF_WAIT=780`.
- Старый обработчик `nut-ob-timer.sh` ожидал 600 секунд, создавал
  `/etc/killpower` и выполнял `systemctl poweroff`.
- Host key Proxmox: ED25519
  `SHA256:D0653AhMxgo4TjNM6Bjvk39XW9DuY+TXAudXmjU8Lmg`.
- На момент обследования работали VM 102/103 и CT 100/101/104/105/106/107.

## Вывод

Референс пригоден для прямого переноса драйверного блока. Повторный подбор
драйверов не требуется. Старый shell-таймер нельзя переносить как основную
логику: он не сохраняет аварийный цикл, его `sleep` не отменяется событием
ONLINE, и он выключает локальный Proxmox вместо управляемого удалённого цикла.
